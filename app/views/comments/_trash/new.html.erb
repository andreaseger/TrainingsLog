<% title "New Comment" %>

<%#= render :partial => 'form' %>
<% form_for [@schedule, Comment.new] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body, :rows => '4', :cols => '128' %>
  </p>
  <p><%= f.submit "Add Comment" %></p>
<% end %>
